import { AppLoader } from "@/components/ui/app-loader";

export default function Loading() {
  return <AppLoader message="Loading HostelHQ..." subMessage="Preparing your verified campus experience" />;
}
